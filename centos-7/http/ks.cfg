install
cdrom
lang en_US.UTF-8
keyboard us
network --onboot yes --device eth0 --bootproto dhcp --noipv6
rootpw --plaintext cloudpack
firewall --enabled --service=ssh
authconfig --enableshadow --passalgo=sha512
selinux --disabled
timezone --utc Asia/Tokyo
bootloader --location=mbr --driveorder=sda --append="crashkernel=auto rhgb quiet"

text
skipx
zerombr

clearpart --all --initlabel
part swap --size=1024
part /boot --size=1024 --label=/boot
part / --size=1024 --grow --label=/

services --disabled=ip6tables,iptables,iscsi,iscsid,lvm2-monitor,netfs,kdump,wpa_supplicant,firewalld,tuned
auth  --useshadow  --enablemd5
user --name=cloudpack --uid=500 --groups=cloudpack,wheel --password=cloudpack
firstboot --disabled
reboot

%packages --ignoremissing
@core
bzip2
sudo
kernel-devel
kernel-headers
-ipw2100-firmware
-ipw2200-firmware
-ivtv-firmware
%end

%post
/usr/bin/yum -y update
echo "cloudpack"|passwd --stdin cloudpack
echo "cloudpack        ALL=(ALL)       NOPASSWD: ALL" >> /etc/sudoers.d/cloudpack
chmod 0440 /etc/sudoers.d/cloudpack
sed -i "s/^.*requiretty/#Defaults requiretty/" /etc/sudoers
%end
